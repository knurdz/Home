# Copy to .env.local for local dev (optional). Appwrite Site already has these set in Console.
NEXT_PUBLIC_USE_APPWRITE_STATIC=1
NEXT_PUBLIC_APPWRITE_ENDPOINT=https://sgp.cloud.appwrite.io/v1
NEXT_PUBLIC_APPWRITE_PROJECT_ID=697b8f42002a34ba04b3
APPWRITE_STATIC_BUCKET_ID=site-static
# APPWRITE_API_KEY=   # server only — upload script + join-us API; never commit
